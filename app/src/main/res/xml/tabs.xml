<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/bottom_nav_home"
        android:title="Home"
        android:enabled="true"
        android:icon="@drawable/ic_home"
        app:cnb_backgroundColor="@color/bg_item"
        app:cnb_iconColor="@color/purple_500"/>

    <item android:id="@+id/bottom_nav_stok"
        android:title="Stok"
        android:icon="@drawable/ic_stok"
        app:cnb_backgroundColor="@color/bg_item"
        app:cnb_iconColor="@color/purple_500"/>


    <item android:id="@+id/bottom_nav_trx"
        android:title="Transaksi"
        android:icon="@drawable/ic_trxx"
        app:cnb_backgroundColor="@color/bg_item"
        app:cnb_iconColor="@color/purple_500"/>

    <item android:id="@+id/bottom_nav_saya"
        android:title="Saya"
        android:icon="@drawable/ic_saya"
        app:cnb_backgroundColor="@color/bg_item"
        app:cnb_iconColor="@color/purple_500"/>

</menu>